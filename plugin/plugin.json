{
  "id": "security_godmode",
  "name": "Security Godmode — LLM Red-Team Lab",
  "version": "0.1.8",
  "description": "Opt-in prompt robustness testing, transformation, refusal scoring, and bounded multi-model comparison for authorized LLM red-team work.",
  "author": "binsarjr",
  "enabled": false,
  "events": [],
  "variables": [
    {
      "name": "AUTO_CONTEXT_ENABLED",
      "label": "Allow per-agent Godmode injection",
      "type": "boolean",
      "default": true,
      "description": "Master switch for direct per-agent system prompt and ephemeral prefill injection."
    }
  ],
  "agent_settings": [
    {
      "name": "enabled",
      "label": "Godmode injection",
      "type": "boolean",
      "default": false,
      "description": "Inject a model-family Godmode profile on every turn without requiring a tool call. Injection remains blocked until the authorization record below is valid."
    },
    {
      "name": "authorization_confirmed",
      "label": "Authorization confirmed",
      "type": "boolean",
      "default": false,
      "description": "Operator attestation that the recorded scope is approved. Godmode fails closed when this is disabled."
    },
    {
      "name": "authorization_scope",
      "label": "Authorization scope",
      "type": "string",
      "default": "",
      "description": "Exact approved targets and activities. User messages cannot expand this stored scope."
    },
    {
      "name": "authorized_by",
      "label": "Authorized by",
      "type": "string",
      "default": "",
      "description": "Operator, owner, or approval ticket responsible for this authorization."
    },
    {
      "name": "authorization_expires_at",
      "label": "Authorization expiry",
      "type": "string",
      "default": "",
      "description": "Required ISO-8601 expiry with timezone, for example 2026-09-09T23:59:59+07:00."
    },
    {
      "name": "system_prompt_mode",
      "label": "System prompt mode",
      "type": "select",
      "options": [
        {"value": "preserve", "label": "Preserve existing prompt"},
        {"value": "append", "label": "Append Godmode prompt"},
        {"value": "override", "label": "Override existing prompt"}
      ],
      "default": "preserve",
      "description": "preserve keeps the compiled Evonic system prompt unchanged; append adds the scoped Godmode prompt; override replaces it for the provider request without writing SYSTEM.md."
    },
    {
      "name": "automatic_discovery",
      "label": "Automatic profile discovery",
      "type": "boolean",
      "default": true,
      "description": "Before the first authorized turn, test and cache the best profile for the exact current model. Discovery runs again after a model change."
    },
    {
      "name": "rediscover_on_next_turn",
      "label": "Rediscover on next turn",
      "type": "boolean",
      "default": false,
      "description": "Run profile discovery once on the next authorized turn, then reset this switch automatically."
    },
    {
      "name": "race_on_failure",
      "label": "Race models after retries fail",
      "type": "boolean",
      "default": false,
      "description": "After the initial response and two retries all refuse, race up to ten other enabled Evonic models. This consumes provider quota."
    },
    {
      "name": "force_transform",
      "label": "Force request transform",
      "type": "boolean",
      "default": false,
      "description": "Transform each new user request with the saved encoding, or L33T when no encoding is saved, while retaining the active system prompt and prefill."
    }
  ]
}
